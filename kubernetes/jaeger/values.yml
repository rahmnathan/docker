# helm install -n jaeger jaeger jaegertracing/jaeger -f jaeger/values.yml
# Gateway API: Create httproute.yml + security-policy.yml if deploying (not currently deployed)

query:
  enabled: true
  oAuthSidecar:
    enabled: false  # Use Gateway API SecurityPolicy instead
  ingress:
    enabled: false