# helm upgrade --install -n swarm-ui swarm-ui ./swarm-ui
# Gateway API: kubectl apply -f swarm-ui/httproute.yml -f swarm-ui/security-policy.yml
#   PREREQ: Create OIDC secret first - see swarm-ui/oidc-secret.yml for instructions

image:
  registry: rahmnathan
  tag: 1.4

persistence:
  capacity:
    data: 100Gi
    output: 100Gi

service:
  timezone: "America/Los_Angeles"