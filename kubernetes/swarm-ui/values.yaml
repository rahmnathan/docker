# Prereq: create namespace and OIDC secret first; see swarm-ui/oidc-secret.yml.
# helm upgrade --install -n swarm-ui swarm-ui ./swarm-ui
# Gateway API HTTPRoute and SecurityPolicy are included in this chart.

image:
  registry: rahmnathan
  tag: 1.4

persistence:
  capacity:
    data: 100Gi
    output: 100Gi

service:
  timezone: "America/Los_Angeles"
