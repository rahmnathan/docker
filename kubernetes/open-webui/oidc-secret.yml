# OIDC client secret for Ollama authentication
# Get the client secret from Keycloak: NathanRahm realm → Clients → ollama → Credentials
#
# Create with:
#   kubectl create secret generic oidc-client-secret -n open-webui --from-literal=client-secret=<secret>
#
# Or apply this file after filling in the base64-encoded secret:
#   echo -n 'your-secret' | base64

apiVersion: v1
kind: Secret
metadata:
  name: oidc-client-secret
  namespace: open-webui
type: Opaque
data:
  client-secret: <base64-encoded-secret>
